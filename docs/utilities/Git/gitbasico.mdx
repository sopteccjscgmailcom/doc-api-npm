---
sidebar_position: 10
---

# Curso Básico de Git  
*Aprende los fundamentos de Git para control de versiones*  

---

## **📌 1. Introducción a Git**  
Git es un **sistema de control de versiones distribuido** que permite rastrear cambios en archivos, trabajar en equipo y mantener un historial de modificaciones.  

### ¿Por qué usar Git? 
✔ Historial completo de cambios.  
✔ Trabajo colaborativo sin perder versiones.  
✔ Posibilidad de revertir errores.  
✔ Ramificación (`branching`) para experimentar sin riesgo.  

---

## **🔧 2. Instalación y Configuración Inicial**  

### Instalar Git
- **Windows**: Descarga [Git for Windows](https://git-scm.com/download/win)  
- **Linux (Debian/Ubuntu)**:  
  ```bash
  sudo apt update && sudo apt install git
  ```
- **Mac**:  
  ```bash
  brew install git  # (con Homebrew)
  ```

### Configuración básica

#### Configuración a nivel de repositorio _(recomendado)_
```bash
git config user.name "sopteccjscgmailcom"
git config user.email "sopteccjsc@gmail.com"
```
#### Configuración a nivel global `--global`
Configuración de `user.name` y `user.email` a nivel global
```bash
git config --global user.name "Tu Nombre"
git config --global user.email "tu@email.com"
git config --global init.defaultBranch main  # Rama principal
```

### Verificar configuración
para ver la configuración actual de Git, ejecuta el siguiente comando y para salir de la lista de configuraciones presiona la tecla `q`.
```bash
git config --list
```

---

## **📂 3. Comandos Básicos**  

### Iniciar un repositorio
```bash
git init          # Crea un repo local
git clone <url>   # Clona un repo remoto (ej. de GitHub)
```

### Estado y cambios
```bash
git status        # Muestra archivos modificados
git add <archivo> # Añade cambios de un unico archivo al staging
git add .         # Añade TODOS los cambios
git reset <archivo> # Quita cambios del área de staging
```

### Guardar cambios (Commit)
-m para agregar un mensaje al commit 
```bash
git commit -m "Mensaje descriptivo"  # Guarda cambios en el historial
```

### Ver historial
```bash
git log                                     # Muestra commits
git log --oneline                           # Historial resumido
git los --graph                             # Muestra el grafo de ramas
git log --graph -decorate --all --oneline   # Muestra el grafo de ramas con codigo hash resumido

```

---

## **🌿 4. Ramas (Branches)**  

### **Crear y cambiar de rama**  
```bash
git branch <nombre>    # Crea una rama
git checkout <nombre>  # Cambia a la rama
git switch <nombre>    # Alternativa moderna
```

### **Fusionar ramas**  
```bash
git merge <rama>  # Fusiona una rama con la actual
```

### **Eliminar rama**  
```bash
git branch -d <rama>  # Borra rama local
```

---

## **🔄 5. Trabajo con Repos Remotos (GitHub, GitLab, etc.)**  

### **Conectar repositorio remoto**  
```bash
git remote add origin <url-repo>  # Ej: https://github.com/usuario/repo.git
```

### **Subir cambios (Push)**  
```bash
git push -u origin main  # Primera vez
git push                # Sube cambios después
```

### **Bajar cambios (Pull)**  
```bash
git pull origin main  # Actualiza tu repo local
```

### **Clonar un repositorio**  
```bash
git clone <url-repo>
```

---

## **🛠 6. Comandos Útiles Extra**  

### **Deshacer cambios**  
```bash
git restore <archivo>  # Descarta cambios en un archivo
git reset --hard       # Elimina TODOS los cambios locales (¡Cuidado!)
```

### **Etiquetas (Tags)**  
```bash
git tag v1.0          # Crea un tag
git push --tags       # Sube tags al remoto
```

### **Alias (Atajos)**  
```bash
git config --global alias.st "status -s"  # git st → git status -s
```

---

## **📚 7. Recursos para Aprender Más**  
- **Git Oficial**: [git-scm.com](https://git-scm.com/)  
- **Práctica Interactiva**: [learngitbranching.js.org](https://learngitbranching.js.org/)  
- **GitHub Guides**: [guides.github.com](https://guides.github.com/)  

---

### **✅ ¡Resumen !**  
1. Crea un repositorio local (`git init`).  
2. Añade un archivo (`git add`).  
3. Haz un commit (`git commit -m "Primer commit"`).  
4. Sube a GitHub/GitLab (`git remote add origin ...` + `git push`).  

🚀 **¡Dominar Git te hará un mejor desarrollador!**