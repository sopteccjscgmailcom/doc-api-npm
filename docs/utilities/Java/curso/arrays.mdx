
# 📋 Arreglos (Arrays) 

## Concepto
📌 **Un array** es una estructura que almacena múltiples valores del mismo tipo.  

🔹 **Ejemplo: Array de números enteros**  

```java
public class ArraysEjemplo {
    public static void main(String[] args) {
        int[] numeros = {10, 20, 30, 40, 50}; // Declaración y asignación

        System.out.println("Primer número: " + numeros[0]); // 10

        // Recorrer el array con un bucle
        for (int i = 0; i < numeros.length; i++) {
            System.out.println("Número en posición " + i + ": " + numeros[i]);
        }
    }
}
```

📌 **Explicación:**  
🔹 `int[] numeros = {10, 20, 30, 40, 50};` → Array de enteros.  
🔹 `numeros[0]` → Accede al primer elemento.  
🔹 `numeros.length` → Devuelve la cantidad de elementos en el array.  
🔹 Bucle `for` → Recorre los elementos del array.  

---
## 🚀Profundizando

Los **arrays** son estructuras de datos que almacenan múltiples valores del mismo tipo en una sola variable. Vamos a profundizar en su uso con más ejemplos y conceptos avanzados.  

---

## **🔹 1. Declaración y Asignación de Arrays**  
Hay dos formas de declarar un array en Java:  

🔹 **Forma 1:** Declaración y asignación directa  
```java
int[] numeros = {10, 20, 30, 40, 50}; 
```

🔹 **Forma 2:** Usando `new` para definir el tamaño  
```java
int[] edades = new int[5]; // Array con 5 posiciones
edades[0] = 18;
edades[1] = 25;
edades[2] = 30;
```

📌 **Nota:** Los arrays en Java tienen un **tamaño fijo** una vez declarados.  

---

## **🔹 2. Recorrer un Array (Bucle `for` y `foreach`)**  
### **📌 Usando `for` tradicional**
```java
int[] numeros = {1, 2, 3, 4, 5};

for (int i = 0; i < numeros.length; i++) {
    System.out.println("Número en índice " + i + ": " + numeros[i]);
}
```

### **📌 Usando `for-each` (más simple)**
```java
for (int num : numeros) {
    System.out.println("Número: " + num);
}
```
📌 **Diferencias:**  
✅ `for` → Nos da acceso al índice (`i`).  
✅ `foreach` → Más limpio, pero no permite modificar los valores del array directamente.  

---

## **🔹 3. Arrays de Cadenas (`String[]`)**  
También podemos almacenar texto en un array:  

```java
String[] nombres = {"Juan", "Ana", "Luis"};

for (String nombre : nombres) {
    System.out.println("Nombre: " + nombre);
}
```

---

## **🔹 4. Arrays Multidimensionales (Matrices)**  
📌 **Un array bidimensional** es un array dentro de otro array (como una tabla).  

🔹 **Ejemplo: Matriz 3x3**  
```java
int[][] matriz = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};

// Acceder a un elemento
System.out.println("Elemento en [1][2]: " + matriz[1][2]); // 6

// Recorrer la matriz
for (int i = 0; i < matriz.length; i++) {
    for (int j = 0; j < matriz[i].length; j++) {
        System.out.print(matriz[i][j] + " ");
    }
    System.out.println(); // Salto de línea
}
```

📌 **Notas:**  
✅ `matriz[1][2]` → Accede a la segunda fila y tercera columna.  
✅ `matriz.length` → Número de filas.  
✅ `matriz[i].length` → Número de columnas por fila.  

---

## **🔹 5. Arrays Dinámicos con `ArrayList`**  
📌 Como los **arrays tienen tamaño fijo**, Java nos ofrece **`ArrayList`** para manejar listas dinámicas.  
🔜 (Lo veremos en **colecciones** más adelante).

```java
import java.util.ArrayList;

ArrayList<Integer> lista = new ArrayList<>();
lista.add(10);
lista.add(20);
lista.add(30);
System.out.println(lista); // [10, 20, 30]
```

---

📌 **¿Listo para avanzar a las colecciones (`List`, `Set`, `Map`)?** 😊